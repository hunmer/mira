# 自定义 Tab Demo

演示客户端插件 API：

- `api.tabs.registerCustomTab(definition)` 注册自定义 Tab。
- `api.tabs.openCustomTab(id, data?)` 打开已注册 Tab。
- `render(container, context)` 使用原生 DOM 构建 UI，并返回清理函数。

插件启用后，点击右侧插件贡献图标即可打开 Demo Tab。
