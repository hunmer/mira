# 2026-08-05

## HomeView UI 重设计（极简玻璃拟态）

- 参考 `Downloads/image.hyhschool.com/image_1785914655896_1.png`（Pixora 风格）产出设计规范：`docs/ui-design-spec.md`（色彩/圆角/阴影/玻璃配方/布局规范）。
- 改造文件（仅样式，不改逻辑、不新建组件）：
  - `src/renderer/views/HomeView/index.vue`：根容器改冷调灰紫渐变底；Header 外包 `px-3 pt-3` 悬浮；主区 `p-3 gap-3`；侧栏/主区 ResizablePanel 改玻璃面板（rounded-2xl + bg-white/60 + backdrop-blur-xl + 柔和投影）；ResizableHandle 改 `w-3 bg-transparent` 作间隔（注意：PanelGroup 上加 gap 会破坏 reka-ui Splitter 尺寸计算）；空状态改玻璃卡片。
  - `HomeHeader.vue`：header 改玻璃悬浮栏；素材库按钮改 `bg-primary/10 text-primary`；Tab 容器 `bg-secondary-100/70 rounded-xl`；"连接服务器" hover 改 `hover:bg-primary/10`。
  - `HomeSidebar.vue`：去掉顶部 border-b；导入按钮改 `h-8 w-8 rounded-lg hover:bg-primary/10`；底部搜索胶囊改 `bg-primary/10 text-primary`。
  - `HomeToolbar.vue`：工具条改玻璃胶囊面板；"退出登录" hover 改 `hover:bg-destructive/10`。
- 验证：`pnpm run build` 通过（11.67s）。
- 待办：中间图片列表/右侧图片信息/底部分页在 TabViewRenderer 内的视图组件中，本次未改，规范已在 md 第 6.3 节定义。

## 样式下落到子组件 + 三列布局重构（同日后续）

- 组件样式下落：FilterBar（激活态 bg-primary/10）、MediaGridItem（卡片 rounded-xl + hover 投影 + 文件名玻璃条）、MediaListComponent（行 hover/selected 改 primary 系、扩展名 chip 胶囊）、MediaDetailComponent（tag/folder chip 改 bg-primary/10）、FolderTreeComponent（树节点选中 bg-primary/10、搜索框胶囊）。
- 三列重构：
  - HomeHeader 重写为紧凑右置栏（w-fit ml-auto），只留导航+窗口控制；素材库切换与 tabs 迁出。
  - 素材库下拉迁入 HomeSidebar 顶部（含 canAccessLibrary/getLibraryLocalPath/openLibraryFolder/onSelectCollection 逻辑与 4 个新 emit）。
  - index.vue：tabs 内联到中间列顶部（ContextMenu 右键保留），激活 tab 用 `rounded-t-2xl border-b-0 -mb-px` 与内容面板同底色连接；tab 条固定 h-[56px]（=Header 44px+gap 12px）使内容面板顶与第三列详情面板顶对齐；滚动条用 `[scrollbar-width:none] [&::-webkit-scrollbar]:hidden` 隐藏。
  - MediaDetailComponent 从 MediaTabListView 迁到 index.vue 第三列（数据源 mediaStore.detailSidebarFiles，同步 watch 仍在 MediaTabListView；显隐由 mediaStore.showDetailSidebar 控制）。
  - MediaTabListView 删除右侧面板与相关 import/handler。
- 磨砂统一降透明度阶梯：内容区 bg-white/30、侧栏/Header/工具条/详情 bg-white/40、状态栏 bg-white/50、浮动栏 bg-white/60；根渐变加深为 #e3e7f7→#dde3f5。
- 注意：build 重试被用户手动中断（疑似 Electron 占用 dist 导致 emptyDir 失败），后续不自动构建，由用户自行验证。

## HomeHeader 头像化 + HomeToolbar 移除 + tab 对齐修复（同日晚）

- HomeHeader 再次重写：导航按钮迁到 index.vue tab 条左侧；原位改为用户头像 Dropdown（上传/插件/快捷键/设置/开发者工具/退出登录全部并入菜单），右侧保留窗口控制。
- HomeToolbar.vue 已删除（功能并入头像菜单，无其他引用）。
- 三列卡片阴影统一加强为 `shadow-[0_12px_40px_rgba(99,102,241,0.10)]`。
- tab 对齐：未激活 tab 与导航按钮组用 `mb-0.5`（2px+6px=8px 基线 = 激活态 py-2），解决选中/未选中水平不对齐。

## 详情面板 Y 对齐修复 + 弹出层玻璃化

- 错位根因：HomeHeader 头像按钮 p-1+w-7=36px 超过 h-8 按钮，header 实际 48px ≠ tab 条假设的 44px。修复：头像按钮改 h-8 w-8 flex 居中，恢复 44px，第三列详情面板与中间内容面板顶重新对齐（56px）。
- shadcn-vue 基础组件玻璃化（全局生效）：PopoverContent、DropdownMenuContent、ContextMenuContent 改 `bg-white/80 backdrop-blur-xl rounded-xl/2xl border-white/60 shadow-[0_12px_40px_rgba(99,102,241,0.12)]`；DialogContent 改玻璃 `rounded-2xl shadow-[0_24px_60px_rgba(99,102,241,0.15)]`；DialogOverlay 改 `bg-black/40 backdrop-blur-sm`。
