# 2026-08-07

- 全局滚动条统一 3px：`src/renderer/assets/main.css` 的 `::-webkit-scrollbar` 已存在 3px 设置，但多个组件（GlobalSearchContent、ImageInfoComponent、ServerEditDialog、VirtualScrollComponent、HomeView、MediaTabListView、Image/VideoThumbnailListComponent）用更高优先级选择器局部覆盖为 4/6/8px。最小改动：给全局 width/height 加 `!important` 强制统一为 3px。刻意 `display:none` 隐藏滚动条的地方（FolderTreeComponent、HomeView 标签栏）不受影响。
