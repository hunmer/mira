{
  "name": "@hunmer/mira-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Mira TypeScript project - monorepo with core, server, and plugins",
  "scripts": {
    "build:core": "pnpm -C packages/mira-app-core run rebuild",
    "build:server": "pnpm -C packages/mira-app-server run rebuild",
    "build:plugins": "pnpm run build:mira_thumb && pnpm run build:mira_user && pnpm run build:upload_statistics",
    "start:server": "pnpm run build:core && pnpm run build:server && pnpm run build:plugins"
  },
  "keywords": [
    "mira",
    "typescript",
    "server",
    "plugins"
  ],
  "author": "hunmer",
  "license": "ISC",
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild",
      "sqlite3",
      "sharp"
    ],
    "overrides": {
      "mira_duplicate_scanner": "link:C:/Users/Administrator/AppData/Local/pnpm/global/5/node_modules/mira_duplicate_scanner"
    }
  },
  "dependencies": {
    "mira_duplicate_scanner": "link:C:/Users/Administrator/AppData/Local/pnpm/global/5/node_modules/mira_duplicate_scanner"
  }
}
