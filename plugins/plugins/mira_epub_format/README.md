# Mira EPUB Format

Mira server/web format plugin for EPUB metadata, cover thumbnails, and reading.

The web reader assets are migrated from Eagle's `epub-format-extension`. The Eagle API is replaced by a small browser compatibility object and Mira's authenticated extra-file URL.
