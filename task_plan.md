# 本地文件夹浏览功能

## 目标
在 mira-client 中增加本地磁盘/文件夹浏览 Tab，支持选择、文件操作、目标目录选择与 Electron 拖拽导出。

## 阶段
- [x] 1. 调查侧栏模块、Tab 注册、文件列表、Electron 文件 API
- [x] 2. 设计最小数据契约与组件落点
- [x] 3. 实现本地文件浏览 Tab、选择及菜单/批量操作
- [x] 4. 实现路径选择对话框与拖拽移动/桌面复制
- [x] 5. 类型检查、测试并修正
- [x] 6. 按项目要求重启持久化服务并交付
- [x] 7. 设计分栏与画廊的数据状态
- [x] 8. 实现分栏与画廊视图
- [x] 9. 回归验证并重启客户端
- [x] 10. 调查目录选择、侧栏持久化与路径规范化能力
- [x] 11. 实现自定义文件夹分组、增删及路径编辑跳转
- [x] 12. 构建验证并重启客户端
- [x] 13. 调查素材库偏好存储与折叠状态生命周期
- [x] 14. 将侧栏模块折叠状态并入素材库本地配置
- [x] 15. 构建验证并重启客户端
- [x] 16. 对比通用画廊/分栏信息面板与本地文件状态
- [x] 17. 为本地画廊和分栏视图补齐文件信息面板
- [x] 18. 构建验证并重启客户端
- [x] 19. 调查上传对话框初始文件与本地路径导入契约
- [x] 20. 固定底部工具栏并实现导入到/批量导入到
- [x] 21. 构建验证并重启客户端
- [x] 22. 调查本地文件四种视图的数据派生与滚动容器
- [x] 23. 实现每页 500 条的滚动追加，并保持筛选排序作用于完整目录
- [x] 24. 构建回归验证并重启客户端
- [x] 25. 设计原生缩略图 IPC 与图标视图接入点
- [x] 26. 实现主进程、preload、共享类型和 renderer 缩略图缓存
- [x] 27. 全量构建验证并重启客户端
- [x] 28. 调查 Tab data 持久化和图标尺寸控制组件
- [x] 29. 实现筛选/视图/尺寸状态恢复与滑轮调节
- [ ] 30. 构建验证并重启客户端

## 约束
- 优先复用现有组件与 IPC，不修改 mira-app-core，除非现状证明必要。
- 不覆盖工作区已有改动。
- 默认不使用真实浏览器测试。

## 错误记录
| 错误 | 尝试 | 处理 |
|---|---:|---|
| 假设 `src/main/handlers/FileSystemHandler.ts` 存在，但路径不存在 | 1 | 使用 `rg --files` 按文件系统处理器名称重新定位 |
| PowerShell 中 `rg` 的引号转义导致正则不完整 | 1 | 改用简单字面量 `rg "vue-sonner"` |
| 更新计划时发现阶段 3 条目重复，补丁上下文未命中 | 1 | 读取计划后合并重复条目 |
| 全量 type-check 失败：1 个新增未使用函数 + 27 个既有错误 | 1 | 删除新增的 `joinPath`；既有错误不在本次范围 |
| `git diff --check` 发现 FolderTreeComponent 空行尾随空格 | 1 | 清理该行后重新验证 |
| 定向 ESLint 无法启动：仓库使用 ESLint 9 但缺少 `eslint.config.*` | 1 | 以成功的 Vite 生产构建、vue-tsc 新增诊断对比和 diff-check 验证 |
| 路径编辑改造中成功分支提前返回，导致 loading 无法复位 | 1 | 改为统一收尾后返回 `loaded` 状态 |
| 首次补丁按旧右键菜单上下文匹配失败 | 1 | 读取当前三处菜单结构后拆分补丁 |
| 全量 `type-check` 失败，报告 28 条仓库其他文件既有诊断 | 1 | 确认无 `LocalFolderTabView.vue` 诊断，继续用生产构建和定向日志核验本次改动 |
| 并行执行构建与类型检查时只返回失败的类型检查输出 | 1 | 单独重跑 `pnpm build` 获取明确构建结果 |
| 暂未执行本次原生缩略图功能构建 | 1 | 新增阶段 27，使用 `pnpm build:all` 验证 main/preload/renderer |
| 全量 `type-check` 仍报告 28 条其他文件诊断 | 1 | 确认本次新增 IPC、preload 和 LocalFolderTabView 无诊断 |

## 初步决策
- 本地任意路径操作不复用面向服务端素材库的 `FileSystemModule`。
- 通过动态 Tab 类型挂载独立本地文件浏览组件。
- IPC 新增 `listRoots/listDirectory/openPath/copyEntries/moveEntries/removeEntries`。
- 本地目录选择对话框复用 `FolderTreeComponent`，增加只读模式隔离素材库操作。
- 导入操作仅导入文件；目录可导航并作为复制/移动/拖放目标。
- 分页只限制筛选排序后的 DOM 渲染数量，不截断 `listDirectory` 返回的完整条目，以保证搜索、过滤、排序仍覆盖当前目录全部内容。
- 列表/图标共用主纵向滚动容器；分栏按每列独立分页；画廊缩略条按横向滚动分页。
