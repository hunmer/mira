# FolderTree 连线迁移

## 目标
用 `LibraryTree.vue` 当前的 SVG 路径连线替换桌面端 FolderTree 的旧行内 div 线段。

## 阶段
- [x] 确认现有实现与 @he-tree DOM 模型
- [x] 实现容器级 SVG 路径和 hover/选中状态
- [x] 删除 FolderTreeNode 旧线段与样式
- [x] 验证交由用户手动完成（用户明确要求不再自动测试）

## 约束
- 保留现有未提交改动和节点交互。
- 不使用真实浏览器测试。
- 用户将自行测试，后续不自动执行类型检查、构建或启动客户端。

## Errors Encountered
- 并行检索包含不存在的根级 `node_modules/@he-tree/vue`，命令返回 1；已改查包内依赖路径。
- `git diff --check` 发现目标文件既有改动混用 CRLF，新增行被判定 trailing whitespace；改用项目格式化工具统一本次涉及文件。

---

# 回收站素材详情只读限制

## 目标
回收站 Item 在客户端详情面板不可编辑，服务端相关写接口同时拒绝回收站 Item。

## 阶段
- [x] 盘点详情编辑入口与对应服务端路由
- [x] 确定统一的回收站判定和错误响应
- [x] 实现前端只读和后端防绕过限制
- [x] 更新 SDK 覆盖审计产物
- [x] 静态核对改动（不自动测试/构建/重启）

## 约束
- 保留已有未提交改动。
- 不使用真实浏览器。
- 遵循用户既有偏好：本轮不自动执行验证调试。

## Errors Encountered
- 首次检查计划文件时向 `Get-ChildItem -Name` 传入数组导致参数类型错误；已改用 `Test-Path` 逐项检查。

---

# 回收/恢复文件名与物理路径一致

## 目标
单文件移入回收站或恢复时，若 `getUniquePath()` 产生了新文件名，同步更新 Item `name`，失败时完整回滚。

## 阶段
- [x] 确认单文件软删除/恢复逻辑与冲突命名规则
- [x] 补充回归测试（仅写入，不自动执行）
- [x] 同步更新 `name/path/recycled` 并补全失败回滚
- [x] 静态核对改动

## 约束
- 保留已有未提交改动。
- 遵循用户既有偏好：不自动执行测试、构建或重启。
- 文件夹整体回收使用独立目录去重，不改变子文件 `name`。
